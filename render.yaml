services:
  - type: web
    name: linkedin-profile-api
    runtime: docker
    plan: free
    region: oregon
    envVars:
      - key: PORT
        value: 3000
      - key: API_KEY
        generateValue: true
      - key: LINKEDIN_LI_AT
        sync: false
